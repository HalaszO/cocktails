import { Typography } from "@material-ui/core";
import "./appTitle.css";

export const AppTitle = () => {
  return (
    <div className="app-title">
      <Typography variant="h2">Cocktail app</Typography>
    </div>
  );
};
